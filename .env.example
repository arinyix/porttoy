# EMAIL / SMTP (Gmail)
MAIL_FROM_EMAIL="COLOQUE_SEU_EMAIL_AQUI"
MAIL_FROM_NAME="ToyLab • UFOPA"

# Para onde o site envia as notificações do Fale Conosco
MAIL_TO_EMAIL="COLOQUE_SEU_EMAIL_AQUI"
MAIL_TO_NAME="ToyLab Admin"

SMTP_HOST="smtp.gmail.com"
SMTP_PORT=587
SMTP_ENCRYPTION="tls"            # "tls" (porta 587) ou "ssl" (porta 465)
SMTP_USERNAME="COLOQUE_SEU_EMAIL_AQUI"
SMTP_PASSWORD="COLOQUE_SEU_APP_PASSWORD_AQUI"  # 16 dígitos do Gmail App Password

# Logs/erros
MAIL_SHOW_ERRORS=false           # true = mostra detalhe técnico nos cards (DEV)
MAIL_SMTP_DEBUG=0                # 0 = sem debug; 2 = verbose (vai para error_log)
MAIL_LOG_DEBUG=true              # sempre registra debug no error_log em caso de falha
